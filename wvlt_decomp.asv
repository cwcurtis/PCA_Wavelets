function dwtc = wvlt_decomp(f0,hflt,nlvls,slvls)
hflt = fftshift(hflt);
%nones = (-1)^(-2^(slvls-1)+1:2^(slvls-1));
%gflt = nones.*hflt;
%gflt = fliplr(gflt);
%gflt = [0 gflt(1:end-1)];
gflt = qmf()
dwtc = zeros(2^(slvls+1)-2^(slvls-nlvls),1);
dwtc(end-(2^slvls-1):end) = f0;

for jj=1:nlvls
    
end
